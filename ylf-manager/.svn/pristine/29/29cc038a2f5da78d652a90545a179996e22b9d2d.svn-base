<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 设置别名 -->
	<typeAliases>
		<!-- 2. 指定扫描包，会把包内所有的类都设置别名，别名的名称就是类名，大小写不敏感 -->
		<package name="com.shun.crm.pojo" />
	</typeAliases>
<!-- <mappers>		
		由于在applicationContext.xml中配置mapper的动态代理了
		所以在这里就不用重复扫描了
		<package name="mapper"/>
	 </mappers> -->
</configuration> 